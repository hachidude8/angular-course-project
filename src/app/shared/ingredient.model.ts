export class Ingredient {

/*
    Declaration shortcut syntax
    By adding public to the params, we state that those are
    properties of this model.
*/

    constructor(public name: string, public amount: number) {
    }
}
